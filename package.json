{
  "name": "causal-inference-101",
  "version": "1.0.0",
  "private": true,
  "description": "A practical, computation-supported introduction to modern causal inference.",
  "scripts": {
    "build": "myst build --html && node scripts/set-production-origin.mjs",
    "dev": "myst start",
    "test": "python3 test_all.py"
  },
  "devDependencies": {
    "mystmd": "1.10.1"
  },
  "engines": {
    "node": "24.x"
  }
}
